/*const prevButton5 = document.getElementById("prevButton");
const nextButton5 = document.getElementById("nextButton");
const imagen_card_1 = document.querySelector(".imagen_card_1");

let currentIndex = 0;

prevButton.addEventListener("click", () => {
  if (currentIndex > 0) {
    currentIndex--;
    scrollToImage();
  }
});

nextButton.addEventListener("click", () => {
  if (currentIndex < imagen_card_1.children.length - 1) {
    currentIndex++;
    scrollToImage();
  }
});

function scrollToImage() {
  const offset = currentIndex * imagen_card_1.offsetWidth;
  imagen_card_1.scrollLeft = offset;
}
*/
